language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-rails-mithril' ]; then cd .. && eval "mv $currentfolder generator-rails-mithril" && cd generator-rails-mithril; fi

